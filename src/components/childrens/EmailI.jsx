import React from "react";
import EmailIcon from "@mui/icons-material/Email";

function EmailI({ size = "12px", color = "blue" }) {
  return <EmailIcon style={{ fontSize: size, color: color }} />;
}

export default EmailI;
